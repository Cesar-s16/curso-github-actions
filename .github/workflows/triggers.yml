name: Triggers Demo

on:
  push:
    branches: ["main"]
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Event Name
        run: echo "El evento que disparó este workflow fue ${{ github.event_name }}"